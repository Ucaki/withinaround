import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['cdn.yogajournal.com'], // replace with your image's domain
  },
};

export default nextConfig;
